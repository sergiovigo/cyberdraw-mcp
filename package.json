{
  "name": "drawio-mcp",
  "version": "2.1.0",
  "description": "Draw.io MCP - A monorepo containing the MCP server and browser plugin",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.8.1",
  "scripts": {
    "audit:dependencies": "corepack pnpm@11.13.0 --pm-on-fail=ignore audit --audit-level=moderate",
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test:watch",
    "test:coverage": "pnpm -r test:coverage"
  },
  "keywords": [
    "mcp",
    "modelcontextprotocol",
    "drawio",
    "diagrams.net"
  ],
  "author": "Ladislav Gazo",
  "license": "MIT",
  "devDependencies": {
    "ts-jest": "29.4.9"
  }
}
