# CyberDraw MCP Installation And User Guide

## Status

AUTHORITATIVE MARKDOWN SOURCE / M22 DRAFT.

This Markdown document is the versioned source of truth for the CyberDraw MCP
installation and user guide.

Generated DOCX and PDF manuals are distribution or release artifacts derived
from this Markdown source. They are not authoritative source documents, should
not be edited as the source of record and should not normally be versioned in
Git.

M22 may add reproducible generation for DOCX/PDF manuals, but that generation
is not implemented by this document.

## Guide Structure

- [macOS guide](macos.md)
- [Ubuntu guide](ubuntu.md)
- [Windows guide](windows.md)
- [Troubleshooting](troubleshooting.md)

## Accepted MVP Capabilities

The accepted MVP is the local-first profile validated by M21:

```text
MCP client
  -> stdio
  -> drawio-mcp-server --editor
  -> loopback HTTP editor
  -> loopback WebSocket bridge
  -> visible draw.io diagram
```

Accepted capabilities:

- install and run the self-contained `drawio-mcp-server` artifact;
- launch the built-in draw.io editor on loopback;
- connect over stdio from an MCP client;
- discover the public MCP tools;
- create a bounded Mermaid flowchart through `cyberdraw_create_diagram`;
- inspect diagrams through public read-only capabilities, including
  `cyberdraw_analyze_structure`;
- reuse verified draw.io assets from a warm cache.

## Planned M22 Capabilities

M22 plans to turn installation and local operations into a supported capability.

Implemented in M22.1/M22.2 and ready for audit:

- versioned installation contract;
- macOS installer wrapper;
- local tarball validation;
- SHA-256 and SHA-512 verification;
- rejection of non-self-contained tarballs;
- rejection of `workspace:*`;
- rejection of private `cyberdraw-*` runtime dependencies;
- default localhost profile;
- explicit LAN profile opt-in;
- Codex configuration backup and update;
- manifest generation;
- doctor/check structured output;
- controlled MCP handshake probe inside doctor;
- `tools/list` probe inside doctor;
- required CyberDraw tool detection;
- port detection;
- residual process detection and post-probe cleanup check;
- upgrade command;
- uninstall command.

Planned, not yet complete:

- Ubuntu installer;
- Windows installer;
- reproducible DOCX/PDF manual generation from Markdown.

Do not treat those planned items as implemented until the relevant M22 evidence
exists.

## Known Limitations

- There is no public npm release tag or GitHub Release declared by this guide.
- Public beta is not ready.
- Remote or authenticated deployment is not supported by the MVP.
- LAN exposure is an explicit additional profile, not the safe default.
- The LAN profile exposes unauthenticated local HTTP and WebSocket surfaces.
- Full draw.io WAR notice review remains pending for public beta standards.
- The current Markdown source does not yet have an automated release manual
  generation pipeline.
- macOS manual product audit for the installer remains pending.

## Manual Artifact Policy

Authoritative source:

- Markdown under `docs/cyberdraw/user-guide/`.

Derived artifacts:

- `docs/Manual_instalacion_y_guia_uso_CyberDraw_MCP_*.docx`;
- `docs/Manual_instalacion_y_guia_uso_CyberDraw_MCP_*.pdf`;
- future release-specific copies, if generated by a reproducible M22 process.

Derived DOCX/PDF artifacts must not be used as the source for future edits. If
they are needed for distribution, regenerate them from the Markdown source and
record the generating command, source commit and checksums in the release
evidence.
